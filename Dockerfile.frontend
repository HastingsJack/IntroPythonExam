FROM python:3.14-slim
WORKDIR /app
COPY pyproject.toml ./
COPY src/ ./src/
RUN pip install --no-cache-dir .
ENV MPLBACKEND=Agg
EXPOSE 8501
CMD ["streamlit", "run", "src/frontend/Home.py", "--server.address", "0.0.0.0", "--server.port", "8501"]
